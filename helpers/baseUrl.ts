const baseUrl = process.env.NODE_ENV === 'production' ? "https://ls-todos-task-hadasserussi.todos-task.vercel.app" : "http://localhost:3000"
// const baseUrl ="http://localhost:3000"
export default baseUrl